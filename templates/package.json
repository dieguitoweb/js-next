{
  "scripts": {
    "prebuild": "flow",
    "build": "BABEL_ENV=jsnext babel src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  { "engines" :
    { "node" : ">=5" }
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-cli": "^6.14.0",
    "babel-plugin-transform-flow-strip-types": "^6.14.0",
    "babel-preset-es2015": "^6.14.0"
  },
  "babel": {
    "env": {
      "jsnext": {
  "presets": ["es2015"],
  "plugins": ["transform-flow-strip-types"]
      }
    }
  }
}
